<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/titleBegin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4.0dip"
        android:text="@string/onboarding_signin_field_password_title"
        android:textAllCaps="true"
        android:textAppearance="@style/TextAppearance.Black"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/resize12sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/password"
        android:layout_width="0.0dip"
        android:layout_height="wrap_content"
        android:layout_marginTop="0.0dip"
        android:fontFamily="sans-serif"
        android:hint="@string/onboarding_signin_field_password_title"
        android:inputType="textPassword"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/resize18sp"
        android:theme="@style/EditTextLabelLight"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleBegin" />
</android.support.constraint.ConstraintLayout>